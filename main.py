# Built-in imports
# Part 1
def reverse(text):
    """
    Reverses a string produced by a word
    Parameter
    ---------
    text: str
        text to be reversed

    Returns
    -------
    str
        The reversed word
    """
    if len(text) == 1:
        return text
    else:
        arrange text reversed

# Part 2
text = (input("Enter a word = "))
def is_palindrome(text):
    """
    Checks a word to see if it is a palindrome
    Parameter
    ---------
    text: str
        text to be checked

    Returns
    -------
    str
        The checked word
    """
if len(text) = 1:    
    return text
elif:
    check if the word reads the same backwards and forwards
else:
    print("This word is not a palindrome.")